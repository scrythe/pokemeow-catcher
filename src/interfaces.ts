export interface Data {
  email: string;
  password: string;
}

export interface User {
  email: string;
  password: string;
}

export interface newBrowser {
  newBrowser: boolean;
  browserWSEndpoint?: string;
}
